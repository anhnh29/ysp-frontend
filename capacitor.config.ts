import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sav.socialmediaapp',
  appName: 'ionic-app-base',
  webDir: 'public'
};

export default config;
